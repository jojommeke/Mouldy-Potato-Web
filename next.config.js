/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/Mouldy-Potato-Web", //insert own repo name after slash
  output: "export", // <=== enables static exports
  reactStrictMode: true,
};

module.exports = nextConfig;
